# Production environment variables for Vercel frontend build
# Empty string = use relative paths (/api/...) so it works on ANY Vercel subdomain.
# In a monorepo deployment, the frontend and backend share the same domain,
# so relative paths always point to the correct Django backend.
VITE_API_URL=
